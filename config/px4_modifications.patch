diff --git a/Tools/flightgear_bridge b/Tools/flightgear_bridge
--- a/Tools/flightgear_bridge
+++ b/Tools/flightgear_bridge
@@ -1 +1 @@
-Subproject commit f47ce7b5fbbb3aa43d33d2be1f6cd3746b13d5bf
+Subproject commit f47ce7b5fbbb3aa43d33d2be1f6cd3746b13d5bf-dirty
diff --git a/Tools/setup/requirements.txt b/Tools/setup/requirements.txt
index e84c676..f17f73d 100644
--- a/Tools/setup/requirements.txt
+++ b/Tools/setup/requirements.txt
@@ -8,7 +8,7 @@ jinja2>=2.8
 jsonschema
 kconfiglib
 lxml
-matplotlib>=3.0.*
+matplotlib>=3.0
 numpy>=1.13
 nunavut>=1.1.0
 packaging
diff --git a/src/modules/commander/Arming/PreFlightCheck/checks/preArmCheck.cpp b/src/modules/commander/Arming/PreFlightCheck/checks/preArmCheck.cpp
index e655208..7c17f91 100644
--- a/src/modules/commander/Arming/PreFlightCheck/checks/preArmCheck.cpp
+++ b/src/modules/commander/Arming/PreFlightCheck/checks/preArmCheck.cpp
@@ -73,11 +73,8 @@ bool PreFlightCheck::preArmCheck(orb_advert_t *mavlink_log_pub, const vehicle_st
 	}
 
 	// manual control mode require valid manual control (rc)
-	if (control_mode.flag_control_manual_enabled && status.rc_signal_lost) {
-		if (report_fail) { mavlink_log_critical(mavlink_log_pub, "Arming denied! manual control lost"); }
-
-		prearm_ok = false;
-	}
+	// RC check disabled for SITL - no physical RC available
+	// if (control_mode.flag_control_manual_enabled && status.rc_signal_lost) { prearm_ok = false; }
 
 	if (status_flags.flight_terminated) {
 		if (report_fail) { mavlink_log_critical(mavlink_log_pub, "Arming denied! Flight termination active"); }
diff --git a/src/modules/commander/Commander.cpp b/src/modules/commander/Commander.cpp
index 4729a19..3383b74 100644
--- a/src/modules/commander/Commander.cpp
+++ b/src/modules/commander/Commander.cpp
@@ -4238,7 +4238,7 @@ void Commander::estimator_check()
 						&& (fabsf(q(3)) <= 1.f);
 	const bool norm_in_tolerance = (fabsf(1.f - q.norm()) <= 1e-6f);
 
-	const bool attitude_valid = (hrt_elapsed_time(&attitude.timestamp) < 1_s)
+	const bool attitude_valid = (hrt_elapsed_time(&attitude.timestamp) < 5_s)
 				    && norm_in_tolerance && no_element_larger_than_one;
 
 	if (_status_flags.attitude_valid && !attitude_valid) {
@@ -4252,7 +4252,7 @@ void Commander::estimator_check()
 	vehicle_angular_velocity_s angular_velocity{};
 	_vehicle_angular_velocity_sub.copy(&angular_velocity);
 	const bool condition_angular_velocity_time_valid = (angular_velocity.timestamp != 0)
-			&& (hrt_elapsed_time(&angular_velocity.timestamp) < 1_s);
+			&& (hrt_elapsed_time(&angular_velocity.timestamp) < 5_s);
 	const bool condition_angular_velocity_finite = PX4_ISFINITE(angular_velocity.xyz[0])
 			&& PX4_ISFINITE(angular_velocity.xyz[1]) && PX4_ISFINITE(angular_velocity.xyz[2]);
 	const bool angular_velocity_valid = condition_angular_velocity_time_valid
diff --git a/src/modules/ekf2/EKF/control.cpp b/src/modules/ekf2/EKF/control.cpp
index 0682e12..179dc9c 100644
--- a/src/modules/ekf2/EKF/control.cpp
+++ b/src/modules/ekf2/EKF/control.cpp
@@ -556,6 +556,8 @@ void Ekf::resetOnGroundMotionForOpticalFlowChecks()
 
 void Ekf::controlGpsYawFusion(bool gps_checks_passing, bool gps_checks_failing)
 {
+	// GPS yaw disabled - no heading sensor
+	return;
 	if (!(_params.fusion_mode & MASK_USE_GPSYAW)
 	    || _control_status.flags.gps_yaw_fault) {
 
diff --git a/src/modules/ekf2/EKF/ekf.cpp b/src/modules/ekf2/EKF/ekf.cpp
index a623f16..66141c7 100644
--- a/src/modules/ekf2/EKF/ekf.cpp
+++ b/src/modules/ekf2/EKF/ekf.cpp
@@ -129,6 +129,7 @@ bool Ekf::initialiseFilter()
 
 	// protect against zero data
 	if (imu_init.delta_vel_dt < 1e-4f || imu_init.delta_ang_dt < 1e-4f) {
+		_is_first_imu_sample = true; // reset so we retry with valid data
 		return false;
 	}
 
@@ -175,13 +176,13 @@ bool Ekf::initialiseFilter()
 	}
 
 	if (_params.mag_fusion_type <= MAG_FUSE_TYPE_3D) {
-		if (_mag_counter < _obs_buffer_length) {
-			// not enough mag samples accumulated
-			return false;
+		if (_mag_counter < 1) {
+				return false;
 		}
 	}
 
-	if (_baro_counter < _obs_buffer_length) {
+	if (false && _baro_counter < 1) {
+		// baro check disabled for WSL2 SITL
 		// not enough baro samples accumulated
 		return false;
 	}
diff --git a/src/modules/ekf2/EKF/imu_down_sampler.cpp b/src/modules/ekf2/EKF/imu_down_sampler.cpp
index 02f43a1..30b0a8b 100644
--- a/src/modules/ekf2/EKF/imu_down_sampler.cpp
+++ b/src/modules/ekf2/EKF/imu_down_sampler.cpp
@@ -1,4 +1,5 @@
 #include "imu_down_sampler.hpp"
+#include <px4_platform_common/log.h>
 
 #include <lib/mathlib/mathlib.h>
 
@@ -60,6 +61,7 @@ void ImuDownSampler::reset()
 	// target dt in seconds safely constrained
 	float target_dt_s = math::constrain(_target_dt_us, (int32_t)1000, (int32_t)100000) * 1e-6f;
 
+	if (_delta_ang_dt_avg < 1e-6f) { _delta_ang_dt_avg = target_dt_s; }
 	_required_samples = math::max((int)roundf(target_dt_s / _delta_ang_dt_avg), 1);
 
 	_target_dt_s = _required_samples * _delta_ang_dt_avg;
diff --git a/src/modules/ekf2/EKF2.cpp b/src/modules/ekf2/EKF2.cpp
index 166a08f..1318702 100644
--- a/src/modules/ekf2/EKF2.cpp
+++ b/src/modules/ekf2/EKF2.cpp
@@ -333,6 +333,7 @@ void EKF2::Run()
 		}
 
 		if (!_callback_registered) {
+			PX4_WARN("EKF2: callback registration failed, retrying...");
 			ScheduleDelayed(10_ms);
 			return;
 		}
@@ -2101,6 +2102,7 @@ int EKF2::task_spawn(int argc, char *argv[])
 					if ((vehicle_mag_sub.advertised() || mag == 0) && (vehicle_imu_sub.advertised())) {
 
 						if (!ekf2_instance_created[imu][mag]) {
+							PX4_INFO("Creating EKF2 multi instance for IMU %d", imu);
 							EKF2 *ekf2_inst = new EKF2(true, px4::ins_instance_to_wq(imu), false);
 
 							if (ekf2_inst && ekf2_inst->multi_init(imu, mag)) {
diff --git a/src/modules/local_position_estimator/sensors/baro.cpp b/src/modules/local_position_estimator/sensors/baro.cpp
index ea61f5c..05e61f4 100644
--- a/src/modules/local_position_estimator/sensors/baro.cpp
+++ b/src/modules/local_position_estimator/sensors/baro.cpp
@@ -6,8 +6,8 @@ extern orb_advert_t mavlink_log_pub;
 
 // required number of samples for sensor
 // to initialize
-static const uint32_t		REQ_BARO_INIT_COUNT = 100;
-static const uint32_t		BARO_TIMEOUT = 100000;	// 0.1 s
+static const uint32_t		REQ_BARO_INIT_COUNT = 1;
+static const uint32_t		BARO_TIMEOUT = 5000000;	// 0.1 s
 
 void BlockLocalPositionEstimator::baroInit()
 {
diff --git a/src/modules/local_position_estimator/sensors/gps.cpp b/src/modules/local_position_estimator/sensors/gps.cpp
index 6995d44..b4435a0 100644
--- a/src/modules/local_position_estimator/sensors/gps.cpp
+++ b/src/modules/local_position_estimator/sensors/gps.cpp
@@ -6,8 +6,8 @@ extern orb_advert_t mavlink_log_pub;
 
 // required number of samples for sensor
 // to initialize
-static const uint32_t		REQ_GPS_INIT_COUNT = 10;
-static const uint32_t		GPS_TIMEOUT = 1000000;	// 1.0 s
+static const uint32_t		REQ_GPS_INIT_COUNT = 1;
+static const uint32_t		GPS_TIMEOUT = 5000000;	// 1.0 s
 
 void BlockLocalPositionEstimator::gpsInit()
 {
diff --git a/src/modules/local_position_estimator/sensors/land.cpp b/src/modules/local_position_estimator/sensors/land.cpp
index abb2fb3..9657332 100644
--- a/src/modules/local_position_estimator/sensors/land.cpp
+++ b/src/modules/local_position_estimator/sensors/land.cpp
@@ -8,7 +8,7 @@ extern orb_advert_t mavlink_log_pub;
 // to initialize
 //
 static const uint32_t		REQ_LAND_INIT_COUNT = 1;
-static const uint32_t		LAND_TIMEOUT = 1000000;	// 1.0 s
+static const uint32_t		LAND_TIMEOUT = 5000000;	// 1.0 s
 
 void BlockLocalPositionEstimator::landInit()
 {
diff --git a/src/modules/sensors/vehicle_air_data/VehicleAirData.cpp b/src/modules/sensors/vehicle_air_data/VehicleAirData.cpp
index 472d228..d03660d 100644
--- a/src/modules/sensors/vehicle_air_data/VehicleAirData.cpp
+++ b/src/modules/sensors/vehicle_air_data/VehicleAirData.cpp
@@ -42,7 +42,7 @@ namespace sensors
 
 using namespace matrix;
 
-static constexpr uint32_t SENSOR_TIMEOUT{300_ms};
+static constexpr uint32_t SENSOR_TIMEOUT{5000_ms};
 
 VehicleAirData::VehicleAirData() :
 	ModuleParams(nullptr),
@@ -239,7 +239,7 @@ void VehicleAirData::Run()
 
 				if (timestamp_sample >= _last_publication_timestamp[instance] + interval_us) {
 
-					bool publish = (time_now_us <= timestamp_sample + 1_s);
+					bool publish = (time_now_us <= timestamp_sample + 10_s);
 
 					if (publish) {
 						publish = (_selected_sensor_sub_index >= 0)
